var assert = require('assert');

describe('integration tests', function() {



  // markdown and copy task output mapper
  it('takes the part after the glob basepath and appends it to the output path');

  // markdown task
  it('accepts a builtin layout');

  it('accepts a custom layout with a path relative to the root of the repo');

  it('renders individual files to HTML');

  // copy task

  it('copies files from one stream to another');




});
